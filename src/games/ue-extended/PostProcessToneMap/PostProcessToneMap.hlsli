#include "../common.hlsli"

// PostProcessTonemap shaders are the shaders that sample the Lutbuilder
// Formally known as "output shaders"
// We dont really do much here, just collecting grain shaders for custom grain to properly toggle grain off